name: Deploy to GitHub Pages (Develop)

on:
    push:
        branches:
            - develop
    workflow_dispatch:

permissions:
    contents: read
    pages: write
    id-token: write

concurrency:
    group: "pages-${{ github.ref }}"
    cancel-in-progress: true

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Setup Node
              uses: actions/setup-node@v4
              with:
                  node-version: "20"
                  cache: "npm"
                  cache-dependency-path: frontend/package-lock.json

            - name: Setup Pages
              uses: actions/configure-pages@v4

            - name: Install dependencies
              run: |
                  cd frontend
                  npm ci

            - name: Build with Next.js for GitHub Pages
              env:
                  NEXT_PUBLIC_API_URL: ${{ secrets.NEXT_PUBLIC_API_URL }}
                  NEXT_PUBLIC_SITE_URL: https://claytonsiby.github.io/VirtuServe
                  NEXT_PUBLIC_CALENDLY_URL: ${{ secrets.NEXT_PUBLIC_CALENDLY_URL }}
              run: |
                  cd frontend
                  # Use the GitHub Pages specific config for static export
                  mv next.config.js next.config.heroku.js
                  mv next.config.pages.js next.config.js
                  npm run build
                  # Restore original config
                  mv next.config.js next.config.pages.js
                  mv next.config.heroku.js next.config.js

            - name: Add .nojekyll file
              run: touch frontend/out/.nojekyll

            - name: Upload artifact
              uses: actions/upload-pages-artifact@v3
              with:
                  path: frontend/out

    deploy:
        runs-on: ubuntu-latest
        needs: build
        permissions:
            pages: write
            id-token: write
        steps:
            - name: Deploy to GitHub Pages
              id: deployment
              uses: actions/deploy-pages@v4
